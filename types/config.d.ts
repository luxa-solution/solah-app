// config.d.ts

// Declaration for the Expo ESLint flat config
declare module "eslint-config-expo/flat";

// Add declarations for other imported config modules if they also throw this error:
declare module "eslint-config-prettier";
declare module "eslint/config";
