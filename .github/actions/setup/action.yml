name: Setup Node environment
description: Set up Node 20 with npm cache and install dependencies. Caller must checkout first.

runs:
  using: composite
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: "20"
        cache: npm

    - name: Install dependencies
      run: npm ci
      shell: bash
